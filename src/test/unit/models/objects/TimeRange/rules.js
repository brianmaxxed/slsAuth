import { instances } from '../../../../utils/schemaTypes';
const { Schema } = instances();

const fields = {
  start: Schema,
  end: Schema,
};

const required = {
};

const indexes = {
};

export default {
  fields,
  required,
  indexes,
};
