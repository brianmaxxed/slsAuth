import c from '../../../src/config/consts';

export default [
  c.DEFAULT, // TODO: remove when i have multi-device enabled.
  c.DESKTOP,
  c.IPAD,
  c.ANDROID_PHONE,
  c.ANDROID_TABLET,
  c.WINDOWS,
  c.MAC,
  c.LINUX,
];
