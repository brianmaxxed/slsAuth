const endpointNames = {
  // search endpoints
  // movies

  // tv

  // series

  // season

  // episodes
  /*
  '/e': /^(\/episode)$/i,
  '/e/t': /^(\/episode\/top\-rated)/i,
  '/e/d/': /^(\/episode\/detail\/)(.*)/i,
  '/e/u': /^(\/episode\/upcoming)$/i,
  '/e/n': /^(\/episode\/now-playing)$/i,
  */

  // person
  '/p': /^(\/person)$/i,
  '/p/d/': /^(\/person\/detail\/)(.*)/i,

  // Discover

};

export default {
  endpointNames,
};
